# Agent 学习日报 - 2026-07-09

## 今日 Agent 学习内容

- 完成 Day 4：搭建最小可运行的 MCP Server。
- 使用 `FastMCP` 暴露了第一个测试工具 `hello_code_search`。
- 通过 MCP Inspector 成功连接本地 MCP Server，并验证工具可以被识别和调用。
- 排查了 Inspector 连接过程中的问题：一开始误用了 HTTP/OAuth 连接方式，导致 `/register` 404；后续改为使用 `mcp dev server.py` 生成的本地连接入口后成功连接。

## 今日学习收获

- 理解了 MCP Server 的基本运行方式：通过标准协议把本地工具暴露给 AI 应用调用。
- 理解了 `@mcp.tool()` 的作用：被装饰的 Python 函数会成为 MCP Client 可发现、可调用的工具。
- 明确了当前阶段的目标不是马上接入 Zoekt，而是先保证 MCP Server、Inspector、Tool 调用链路跑通。
- 对后续 `code-search-mcp` 的方向更清晰：先实现 `search_code`，再逐步增加 `get_file_context`、`find_symbol`、`find_references` 等能力。

## 今日工作内容

- 完成 cuenta 接入消息条和订单页的联调。
- 调研通过 PySpark 方式接入 Odin 数据，并成功获取到监控数据。

## 明日计划

- 开始 Day 5：将 Zoekt 搜索能力接入 MCP Server。
- 设计并实现第一个真实工具 `search_code(query: str, limit: int = 10)`。
- 让 `search_code` 返回文件路径、行号、代码片段等基础信息。
- 更新 README 和 learning.md，记录 MCP 接入 Zoekt 的设计思路与遇到的问题。
